funcion esPar(numero) {
    retornar numero % 2 == 0;
}

funcion iniciar() {
    var contador = 0;
    var numero = 1;

    mientras (contador < 5) {
        si (esPar(numero)) {
            consola.escribir(numero);
            contador = contador + 1;
        } sino {
        	consola.escribir("no es par");
        }

        numero = numero + 1;
    }
}

iniciar();
