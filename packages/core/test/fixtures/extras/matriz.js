const matriz = Array.of([])

matriz.push('test')

if (matriz.includes('test')) {
  matriz.pop()
}

matriz.length;

matriz.find((item) => item === 'test')

Array.from([1, 2, 3])

Array.fromAsync([1, 2, 3])

Array.isArray([])

Array.of([1, 2, 3])
