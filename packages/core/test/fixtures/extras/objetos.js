const numero = Number(1);

const cadena = String('Hola');

const booleano = Boolean(true);

const objeto = new Object({});

const matriz = new Array([[1, 2], [3, 4]]);

const f = Function(() => {});

const simbolo = Symbol('Simbolo');

const fecha = new Date();

const error = Error('Error');

const expReg = RegExp(/ExpReg/);

const mapa = new Map([['clave', 'valor']]);

const conjunto = new Set([1, 2, 3]);

const apoderado = new Proxy({}, {});

const enteroGrande = BigInt(1n);

const documento = Document(document);
